# LuminOS Security & Hardening Parameters

# Hide kernel pointers from unprivileged users
kernel.kptr_restrict = 2

# Restrict dmesg kernel log inspection
kernel.dmesg_restrict = 1

# Disable unprivileged eBPF access
kernel.unprivileged_bpf_disabled = 1

# Protect against hardlink & symlink attacks
fs.protected_hardlinks = 1
fs.protected_symlinks = 1
fs.protected_fifos = 2
fs.protected_regular = 2

# Restrict ptrace process memory inspection (Yama)
kernel.yama.ptrace_scope = 2

# Network Security - Reverse Path Filtering against IP spoofing
net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.default.rp_filter = 1

# Disable ICMP Redirect Acceptance
net.ipv4.conf.all.accept_redirects = 0
net.ipv6.conf.all.accept_redirects = 0

# Enable ASLR (Address Space Layout Randomization)
kernel.randomize_va_space = 2
